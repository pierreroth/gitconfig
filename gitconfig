[user]
    email = pierre.roth@legrand.fr
    name = Pierre ROTH
[alias]
    co = checkout
    ci = commit
    st = status -sb
    br = branch
    unstage = reset HEAD --
    untrack = rm --cached
    ds = diff --staged
    ls = log --pretty=format:'%C(yellow)%h %C(blue)%ad %C(red)%d %C(reset)%s %C(green)[%cn]' --decorate --date=short
    graph = log --graph --pretty=format':%C(yellow)%h%Cblue%d%Creset %s %C(white) %an, %ar%Creset'
[core]
    autocrlf = input
[color]
    ui = true
[push]
    default = upstream
[pull]
    rebase = preserve
[credential]
    helper = cache --timeout=3600
[status]
    showUntrackedFiles = all
