[user]
    email = pierreroth64@gmail.com
    name = Pierre Roth
[alias]
    co = checkout
    ci = commit
    st = status -sb
    br = branch
    unstage = reset HEAD --
    untrack = rm --cached
    ds = diff --staged
    ls = log --pretty=format:'%C(yellow)%h %C(blue)%ad %C(red)%d %C(reset)%s %C(green)[%an]' --decorate --date=short
    graph = log --graph --pretty=format':%C(yellow)%h%Cblue%d%Creset %s %C(white) %an, %ar%Creset'
[core]
    autocrlf = input
[color]
    ui = true
[push]
    default = upstream
[pull]
    rebase = preserve
[credential]
    helper = cache --timeout=3600
[status]
    showUntrackedFiles = all
    submoduleSummary = true
[diff]
    submodule = log

